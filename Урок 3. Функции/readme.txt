# 1. Реализовать функцию, принимающую два числа (позиционные аргументы)
# и выполняющую их деление. Числа запрашивать у пользователя, предусмотреть
# обработку ситуации деления на ноль.

def my_func(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return 'На 0 делить нельзя'

a = int(input('Введите число 1: '))
b = int(input('Введите число 2: '))
print(my_func(a, b))


# 2. Реализовать функцию, принимающую несколько параметров,
# описывающих данные пользователя: имя, фамилия, год рождения, город проживания, email, телефон.
# Функция должна принимать параметры как именованные аргументы.
# Реализовать вывод данных о пользователе одной строкой.



def my_func(name, surname, year, city, email, tel):
    return f'Имя: {name}, \nФамилия: {surname}, \nГод рождения: {year},' \
           f' \nГород проживания: {city}, \nEmail: {email}, \nТелефон: {tel}'

print(my_func(name = 'Дмитрий', surname = 'Кусов', year = '1992',
              city = 'Москва', email = 'dmitriylyc@gmai.com', tel = '+7(999)999999'))


# 3. Реализовать функцию my_func(),
# которая принимает три позиционных аргумента,
# и возвращает сумму наибольших двух аргументов.

def my_func(a, b, c):
    return sum([a, b, c]) - min([a, b, c])

print(my_func(99, 101, 55))


# 4.  Программа принимает действительное положительное число x и целое отрицательное число y.
# Необходимо выполнить возведение числа x в степень y.
# Задание необходимо реализовать в виде функции my_func(x, y).
# При решении задания необходимо обойтись без встроенной функции возведения числа в степень.

# -------------- 1 вариант ---------------
def my_func(x, y):
    return x ** y if y < 0 and x > 0 else 'x - должно быть положительным числом ' \
                                          '\nу - должно быть отрицательным числом'

print(my_func(4, -3))

# -------------- 2 вариант ---------------
def my_func_2(x, y):
    result = x*x
    if y < -1 and x > 0: # проверка на то чтобы числа подходили для функции
        for i in range(abs(y)-2): # тут отнимаем цифру 2, т.к. возведение во вторую степень уже выполнили в result
            result *= x
        return 1 / result
    else:
        print('x - должно быть положительным числом, больше 0 ' \
        '\nу - должно быть отрицательным числом, меньше -1')


print(my_func_2(4, -3))


# 5. Программа запрашивает у пользователя строку чисел, разделенных пробелом.
# При нажатии Enter должна выводиться сумма чисел.
# Пользователь может продолжить ввод чисел, разделенных пробелом и снова нажать Enter.
# Сумма вновь введенных чисел будет добавляться к уже подсчитанной сумме.
# Но если вместо числа вводится специальный символ, выполнение программы завершается.
# Если специальный символ введен после нескольких чисел, то вначале нужно добавить
# сумму этих чисел к полученной ранее сумме и после этого завершить программу.

def my_func():
    result = 0
    q = 0 # переменная для того чтобы остановить цикл когда for in наткнётся на q
    while q != 1:
        numbers = input('Введите числа разделенные пробелом: ').split(' ')
        try:
            for i in numbers:
                if i in ['q', 'Q']:
                    print(f'Вы вышли из программы, результат составил: {result}')
                    q += 1
                    break
                else:
                    result += int(i)
            # result += sum(int(i) for i in numbers)
            print(f'Результат: {result}')
        except ValueError:
            print(f'Вы ввели недопустимый символ. \nРезультат: {result}')

my_func()



# 6. Реализовать функцию int_func(),
# принимающую слово из маленьких латинских букв и возвращающую его же,
# но с прописной первой буквой. Например, print(int_func(‘text’)) -> Text.

# Продолжить работу над заданием.
# В программу должна попадать строка из слов,
# разделенных пробелом. Каждое слово состоит из латинских букв в нижнем регистре.
# Сделать вывод исходной строки, но каждое слово должно начинаться с заглавной буквы.
# Необходимо использовать написанную ранее функцию int_func().


def int_func(text):
    return text.capitalize()

words = input('Введите фразу: ').split()
result = ''
if words.islower():
    for i in words:
        result += int_func(i) + ' '
else:
    print('Вы ввели заглавные буквы')

print(result)